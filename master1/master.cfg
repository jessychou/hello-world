# -*- python -*-
# ex: set filetype=python:

from buildbot.plugins import *
from twisted.internet import defer


# This is a sample buildmaster config file. It must be installed as
# 'master.cfg' in your buildmaster's base directory.

# This is the dictionary that the buildmaster pays attention to. We also use
# a shorter alias to save typing.
c = BuildmasterConfig = {}

####### WORKERS

# The 'workers' list defines the set of recognized workers. Each element is
# a Worker object, specifying a unique worker name and password.  The same
# worker name and password must be configured on the worker.
c['workers'] = [worker.Worker("example-worker2", "pass")]

# 'protocols' contains information about protocols which master will use for
# communicating with workers. You must define at least 'port' option that workers
# could connect to your master with this protocol.
# 'port' must match the value configured into the workers (with their
# --master option)
c['protocols'] = {'pb': {'port': 9999}}

####### CHANGESOURCES

# the 'change_source' setting tells the buildmaster how it should find out
# about source code changes.  Here we point to the buildbot version of a python hello-world project.

c['change_source'] = []
c['change_source'].append(changes.GitHubPullrequestPoller(
        owner='jessychou',
        repo='hello-world',
        token='f751c7f80d9183a7848520333fd8d0d5a677a256',
        pollInterval=10))

####### SCHEDULERS

# Configure the Schedulers, which decide how to react to incoming changes.  In this
# case, just kick off a 'runtests' build

c['schedulers'] = []
c['schedulers'].append(schedulers.SingleBranchScheduler(
                            name="all",
                            change_filter=util.ChangeFilter(branch='jessydev'),
                            treeStableTimer=None,
                            builderNames=["runtests"]))
c['schedulers'].append(schedulers.ForceScheduler(
                            name="force",
                            builderNames=["runtests"]))
# Test try scheduler: allow developers trigger builds before committing to a vsc repo
from buildbot.scheduler import Try_Userpass
c['schedulers'].append(Try_Userpass(
                                    name='try',
                                    builderNames=['runtests'],
                                    port=5555,
                                    userpass=[('sampleuser','samplepass')]))####### BUILDERS

# The 'builders' list defines the Builders, which tell Buildbot how to perform a build:
# what steps, and which workers can execute them.  Note that any particular build will
# only take place on one worker.

from twisted.internet import defer
from buildbot.steps.shell import ShellCommand, CompositeStepMixin
from os.path import splitext

# ------------------------------------------------------------
#
# Special version of the ShellCommand step allowing to add
#	html log files.
#
class ShellCommandHtml(ShellCommand, CompositeStepMixin):
	html_logfiles = {}

	def __init__(self, **kwargs):
		self.html_logfiles = {}

		if kwargs['logfiles'] is not None:
			logfiles = {}

			for logname, remotefilename in kwargs['logfiles'].items():
				_, extension = splitext(remotefilename)
				if extension.lower() in ('.html', '.htm'):
					self.html_logfiles[logname] = remotefilename
				else:
					logfiles[logname] = remotefilename

			kwargs['logfiles'] = logfiles

		super().__init__(**kwargs)

	@defer.inlineCallbacks
	def commandComplete(self, cmd):
		# process all html files we are aware of
		for logname, remotefilename in self.html_logfiles.items():
			# get the content of the remote file
			html = yield self.getFileContentFromWorker(filename=remotefilename, abandonOnFailure=True)

			# add the html file
			yield self.addHTMLLog(name=logname, html=html)

		# remove html lof files
		self.html_logfiles = {}

		return super().commandComplete(cmd)

factory = util.BuildFactory()
# check out the source
factory.addStep(steps.Git(repourl='https://github.com/jessychou/hello-world.git', mode='incremental'))
# run the tests (note that this will require that 'trial' is installed)
factory.addStep(ShellCommandHtml(
        name            = "unittest",
        description     = "running unit tests",
        command         = ["pytest", "--cov=hello/", "--cov-report=html:./html"],
        logfiles        = {
            "unittestlog": "index.log",
            "resultshtml": "index.html",
        },
        workdir         = "build/html"
    ))

#factory.addStep(steps.ShellCommand(command=["pytest", "--cov=hello/", "--cov-report=html:./html"],
#                                   env={"PYTHONPATH": "."}))

c['builders'] = []
c['builders'].append(
    util.BuilderConfig(name="runtests",
      workernames=["example-worker2"],
      factory=factory))

####### BUILDBOT SERVICES

# 'services' is a list of BuildbotService items like reporter targets. The
# status of each build will be pushed to these targets. buildbot/reporters/*.py
# has a variety to choose from, like IRC bots.

c['services'] = []


@util.renderer
@defer.inlineCallbacks
def getresults(props):
    all_logs=[]
    master = props.master
    steps = yield props.master.data.get(
        ('builders', props.getProperty('buildername'), 'builds',
        props.getProperty('buildnumber'), 'steps'))
    for step in steps:
        if step['results'] == util.Results.index('failure'):
            logs = yield master.data.get(("steps", step['stepid'], 'logs'))
            for l in logs:
                all_logs.append('Step : {0} Result : {1}'.format(
                                    step['name'], util.Results[step['results']]))
                all_logs.append('```')
                l['stepname'] = step['name']
                l['content'] = yield master.data.get(("logs", l['logid'], 'contents'))
                step_logs = l['content']['content'].split('\n')
                include = False
                for i, sl in enumerate(step_logs):
                    all_logs.append(sl[1:])
                all_logs.append('```')
    print("="*20)
    print(f'getresults all_logs: {all_logs}')
    print("="*20)
    return '\n'.join(all_logs)

from buildbot.reporters.generators.build import BuildStartEndStatusGenerator, BuildStatusGenerator
from buildbot.reporters.message import MessageFormatterRenderable
from buildbot.reporters.github import GitHubCommentPush
from github import MyPrCommentsPush

print("come ti generator " + "="*20)
generator = BuildStatusGenerator(message_formatter=MessageFormatterRenderable(getresults))
githubAPIToken = 'f751c7f80d9183a7848520333fd8d0d5a677a256'
c['services'].append(MyPrCommentsPush(token=githubAPIToken, generators=[generator]))

####### PROJECT IDENTITY

# the 'title' string will appear at the top of this buildbot installation's
# home pages (linked to the 'titleURL').

c['title'] = "Hello World CI/CD Jessy"
c['titleURL'] = "https://github.com/jessychou/hello-world.git"

# the 'buildbotURL' string should point to the location where the buildbot's
# internal web server is visible. This typically uses the port number set in
# the 'www' entry below, but with an externally-visible host name which the
# buildbot cannot figure out without some help.

c['buildbotURL'] = "http://localhost:8010/"

# minimalistic config to activate new web UI
c['www'] = dict(port=8010,
                plugins=dict(waterfall_view={}, console_view={}, grid_view={}))

####### DB URL

c['db'] = {
    # This specifies what database buildbot uses to store its state.
    # It's easy to start with sqlite, but it's recommended to switch to a dedicated
    # database, such as PostgreSQL or MySQL, for use in production environments.
    # http://docs.buildbot.net/current/manual/configuration/global.html#database-specification
    'db_url' : "sqlite:///state.sqlite",
}
